services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: dzapp
      POSTGRES_PASSWORD: dzapp
      POSTGRES_DB: dzapp
    ports:
      - "5432:5432"
    volumes:
      - dzapp_db_data:/var/lib/postgresql/data

volumes:
  dzapp_db_data:
